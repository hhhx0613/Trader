1. 当前项目已经存在 Git 仓库并且已经上传过 GitHub。本次任务是更新现有仓库，不要重新执行 git init，不要重新创建 GitHub Repository，不要删除 .git，不要修改已有 Git 历史，也不要执行 force push。
2. 首先检查当前 Git 仓库状态，包括当前分支、remote、是否存在未提交修改以及本地分支与远程分支的关系。确认当前工作仓库和远程仓库没有指向错误项目。
3. 使用 Git 状态和差异检查本次实际发生了哪些变化，并把变化分成：新增文件、修改文件、删除文件、未跟踪文件。先用自然语言给我总结本次代码变化，不要马上 commit 或 push。
4. 对本次新增和修改内容重新执行安全检查，重点检查 API Key、Token、密码、Cookie、Secret、私钥、.env、个人隐私、本地绝对路径、调试日志、临时文件、大型生成文件和测试垃圾文件。尤其检查本次新增文件有没有绕过原有 .gitignore。
5. 如果项目功能、安装方法、环境变量、运行命令、输入输出、目录结构或依赖发生变化，检查 README 是否需要同步更新。README 必须始终与当前代码真实功能一致。如果没有发生相关变化，不要为了制造提交内容而无意义修改 README。
6. 检查依赖文件是否需要更新。例如新增 Python 包后更新对应依赖声明，新增 Node 依赖后确认 package.json 和锁文件一致。不要手工虚构依赖版本。
7. 先向我输出“本次 GitHub 更新检查报告”，包括修改内容、安全检查结果、README 是否需要更新、依赖是否变化、建议提交哪些文件，以及建议的 commit message。这个阶段不要执行 commit 和 push。
8.得到我确认后，再把经过确认的文件加入暂存区。检查暂存后的实际 diff，确认提交内容准确且没有敏感信息。
9. 创建一次清晰的 Git commit。提交信息使用“简短标题 + 详细正文”：标题如 `feat: ...`、`fix: ...`、`docs: ...`、`refactor: ...` 或 `chore: ...`，正文按功能/行为变化、数据或实验、文档与治理、验证情况和已知限制分组说明本次真实变化。不要使用笼统的 `update`，也不要为格式歪曲提交性质。
10. 在 push 前检查远程分支是否有新的提交。如果存在远程更新，先分析是否需要安全同步，避免直接覆盖他人的内容。如果出现 merge conflict，不要擅自选择某一方覆盖，先向我报告冲突文件和原因。
11. 确认没有冲突后，把当前分支正常 push 到已有 GitHub origin。禁止使用 --force 或 --force-with-lease，除非我明确要求并且已经理解风险。
12. push 完成后报告：当前分支、 commit hash 、commit message、修改文件数量、是否成功 push、远程仓库地址，以及是否仍存在未提交文件。

## 回测记录检查

13. 若本次提交包含回测结果，确认 `docs/experiment_index.csv` 和自动生成的 `docs/backtest_report.md` 已随回测更新并纳入暂存；无需另建推送台账。
