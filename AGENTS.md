# AI Agent 开发原则

## 核心思想

- **第一性原理思考**: 理解需求背后的真实目标，而不是直接套用已有模式或技术方案
- **本质问题优先**: 优先解决本质问题，避免为假设中的未来需求提前设计复杂系统
- **简单可靠原则**: 在保证长期可维护性的前提下，选择当前最简单、可靠、清晰的实现方案

## 简洁与设计原则

- **KISS 原则 (Keep It Simple, Stupid)**: 优先选择简单直接的实现，避免不必要的复杂度
- **DRY 原则 (Don't Repeat Yourself)**: 避免重复逻辑，但不要为了消除少量重复而创建过度抽象
- **SOLID 思想**: 保持职责清晰、降低模块耦合，提高代码可维护性和扩展能力

## 架构原则

- **删除废弃方案**: 不要为了保持向后兼容而长期保留废弃方案。优先删除过时代码，而不是增加兼容层、fallback 或临时迁移逻辑
- **拒绝未经验证的设计**: 避免提前引入抽象、配置和间接层
- **演进式开发**: 从最小可工作的版本开始，逐步演进系统。每次修改都应该建立在已有可运行系统之上
- **拒绝过度设计**: 永远不要用未来可能需要的复杂性，牺牲当前产品的可用性

## 代码质量原则

- **明确职责**: 保持模块职责明确，避免一个模块承担过多职责
- **成熟依赖**: 优先使用成熟、稳定、维护良好的第三方库，而不是重复造轮子
- **谨慎新增**: 使用项目已有依赖解决问题之前，不要随意新增依赖
- **预先调研**: 在引入新方案前，先检查已有代码、依赖、文档和能力
- **拒绝虚荣**: 避免为了"看起来更优雅"而增加实际复杂度


## 工程决策原则

- **长期可维护性优先**: 优先选择长期可维护的方案，而不是只能临时运行的解决方案
- **业务目标导向**: 代码应该服务于业务目标，而不是为了展示技术复杂度
- **拒绝过度升级**: 如果简单方案已经满足需求，不要主动升级为复杂方案

---

## 项目特定规范

### 1. 修改授权机制

需要改变架构/功能时，未经用户明确同意，不得：
1. 修改任何代码文件
2. 启动回测/实验脚本
3. 执行任何会改变项目状态的操作

**正确做法**: 先说明打算做什么改动，等用户明确说"可以"/"好"/"改吧"等确认词后再动手。小 bug 可以未经允许直接修改

### 2. 记忆与缓存协同

- L1 记忆管理置信度滚动校准机制
- SQLite 缓存用于llm存储
- 定期清理无用缓存、脚本文件（环境变量控制）

### 3. 文档优先级

- Plan.md 为唯一权威架构文档
- 代码改动前需先更新 Plan.md，改动后 review 确认是否遵循
- API 文档仅记录公开接口，定期更新
- 代码注释应解释"为什么"而非"做什么"
- **禁止自动生成报告**: 执行回测或实验后，禁止自动创建 report/summary 等 MD 文件，除非用户明确要求
- README 必须由 Agent 随功能、运行方式、依赖、输出或目录结构变更主动同步，不需要用户另行提醒。

### 4. 实验治理流程

1. Plan.md 驱动开发三步流程
2. 所有变更需考虑对现有功能影响

### Git 提交与推送规则
每次涉及到Git提交与推送时，请阅读"GIT.md"
