# 项目待修改清单

> 审阅日期：2026-09-22  
> 审阅范围：`docs/Plan_v2.md`、`docs/Plan.md`、现有代码、测试与 `output/` 实验产物。  
> 说明：`Plan_v2.md` 是当前权威文档；`Plan.md` 已归档，本清单仍记录两者及实现之间的遗留冲突。

## P0：结论有效性与实验正确性

- [ ] **重新验证选股 alpha 审计中 LLM 结果全为空的问题。**  
  `output/audit_selection_alpha_20260914_000112` 中 LLM 的 IC、收益、换手均为 `NaN`，且有效周数为 0。该审计不能用于得出“LLM 无 alpha”的结论。应先定位 LLM 信号未进入审计数据集的原因，再按同一口径比较 LLM、VADER、20 日动量和随机排序。

- [x] **修复 PPO 环境的周内前视偏差。**  
  环境现以调仓日收盘形成观测，首个持有日只计下一交易日 `open→close`，后续计 `前收盘→当收盘`；真实回测的调仓买卖也已统一按下一交易日开盘撮合。

- [x] **修复风控错误清仓。**  
  R2 已改为仅按开盘撮合后至当日收盘的回撤禁买、次日恢复；R1/R1a 单股止损已改为只清触发标的，不再误清整个组合。

- [x] **输出 PPO 回撤诊断。**  
  回测现落盘调仓级目标暴露、决策前实际暴露、账户回撤与 R2 状态。最新 GPU 回测显示：52 次调仓中 PPO 有 46 次目标暴露为 100%；最大回撤 12.71% 发生于 2026-03-27，之前 2026-03-23 与 03-30 的目标暴露仍为 75%，R2 均允许买入。说明高回撤主要来自 PPO 长期高暴露，而非 R2 锁仓。

- [ ] **完成 Plan 要求的 PPO Go/No-Go 验收。**  
  当前仅有一个周频 PPO 模型与一个最终样本外窗口。需要至少 3 个随机种子、滚动样本外窗口、逐周相对公式仓位收益，以及 block bootstrap 的相对收益和夏普差异区间。

- [x] **统一 PPO 训练环境与真实回测的成本和风控口径。**  
  环境现按逐资产目标权重收取滑点、换手惩罚与固定佣金，维护虚拟持仓/入场价，并模拟 R1、ATR 单股止损和 R2 当日禁买；止损后的实际权重进入后续收益与换手。唯一有意保留的边界是 Stage 1 不模拟 LLM 选股及其换股成本，已在 Plan_v2 明确。

## P1：可复现性、治理与架构一致性

- [~] **让回测入口自动写入完整 `manifest.json`。**  
  已自动生成 summary/equity/trades/decisions/report/manifest，并记录代码版本、参数、数据范围、股票池、模型路径与 hash、成本、研究目的/改动/局限性、训练 seed、工作区脏状态和输入数据指纹；仍缺依赖版本快照。

- [ ] **建立 PPO 模型的可复现性元数据。**  
  `models/ppo_stage1.zip` 未绑定训练数据快照、配置、随机种子、依赖版本和代码提交；已有实验 manifest 还标注了运行时实现未提交。模型必须附带独立元数据，避免仅依赖文件名。

- [ ] **将已冻结的 Stage 2 Residual LLM Adapter 从主路径隔离。**  
  当前权威 Plan 要求 Gate 前不开展 Adapter，但 CLI、实现、测试与历史文档仍将 Stage 2 暴露为可执行主路线。应从默认入口和 README 移除，仅作为明确标注的历史消融材料保留。

- [~] **统一项目文档。**  
  README 已同步 PPO 当前 Gate 状态、运行入口和实验输出；仍需在 alpha 审计与 PPO Go/No-Go 得出正式结论后，统一历史报告和论文表述。

- [ ] **将选股 alpha 审计纳入实验索引与正式决策。**  
  回测实验已迁移至 `docs/experiment_index.csv`；alpha 审计仍遗漏，且 `output/audit/` 与 `output/audit_selection_alpha_*` 存在格式和结论不一致。应明确有效版本、淘汰无效产物，并把可审阅结论写入索引和权威 Plan。

- [ ] **下调 L1 记忆“+73% 有效”的表述。**  
  Plan_v2 与 README 将其描述为已验证能力，但这只是历史单窗口消融，且当前 LLM alpha 审计本身无有效结果。应标注为待跨窗口复核的历史观察，不能作为已证实系统能力。

## P2：实现语义与测试质量

- [x] **修正风险控制的命名或实现。**  
  `MAX_DAILY_DRAWDOWN` 现按开盘撮合完成后的当日权益计算，隔日自动恢复；单股止损不再清空整个组合。

- [~] **补齐关键确定性测试。**  
  已新增 R2 隔日恢复、单股 ATR 止损的回归断言；仍缺 PIT 时间边界、T+1、成本、manifest 与 LLM 审计离线测试。

- [ ] **清理或归档废弃设计残留。**  
  多 Agent、IBKR、实验注册表与 Stage 2 等设计在 Plan_v2、README、CLI 和实现中仍有主路径痕迹。按当前 Plan 的 Gate 原则，应删除其主架构入口，或清晰标记为历史归档。

## 建议处理顺序

1. 修复 alpha 审计 LLM 空值与 PPO 前视偏差。
2. 补齐成本/风控口径和实验元数据。
3. 执行 PPO 与选股 alpha 的正式 Gate 验收。
4. 根据 Gate 结果保留或移除 PPO、LLM 主选股与相关冻结模块。
5. 最后统一 README、Plan、实验索引和历史归档。
